# ===========================================
# CALM Admin - Development Configuration
# ===========================================
# For production, use application-prod.properties with environment variables
# ===========================================

# Server Configuration
server.port=8080

# Database Configuration (H2 for development)
spring.datasource.url=jdbc:h2:file:./data/calm_admin
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# AWS S3 Configuration - Metadata Bucket
aws.s3.metadata.bucket=${AWS_METADATA_BUCKET:vonage-pruebas}
aws.s3.metadata.region=${AWS_METADATA_REGION:us-east-1}
aws.s3.metadata.accessKeyId=${AWS_S3_METADATA_ACCESS_KEY:}
aws.s3.metadata.secretAccessKey=${AWS_S3_METADATA_SECRET_KEY:}
aws.s3.metadata.prefix=recorder/company-calm-953/

# AWS S3 Configuration - Transcriptions Bucket (legacy)
aws.s3.transcriptions.bucket=${AWS_TRANSCRIPTIONS_BUCKET:poc-video-aws}
aws.s3.transcriptions.region=${AWS_TRANSCRIPTIONS_REGION:us-east-1}
aws.s3.transcriptions.accessKeyId=${AWS_S3_ACCESS_KEY:}
aws.s3.transcriptions.secretAccessKey=${AWS_S3_SECRET_KEY:}
aws.s3.transcriptions.prefix=calm/transcripciones/

# AWS S3 Configuration - New Recordings Bucket (June 2026+)
aws.s3.newbucket.bucket=${AWS_S3_NEW_BUCKET:calm-grabaciones}
aws.s3.newbucket.region=${AWS_S3_NEW_REGION:us-east-1}
aws.s3.newbucket.accessKeyId=${AWS_S3_NEW_ACCESS_KEY:}
aws.s3.newbucket.secretAccessKey=${AWS_S3_NEW_SECRET_KEY:}
aws.s3.newbucket.prefix=recorder/company-calm-953/

# OpenAI Configuration
openai.api.key=${OPENAI_API_KEY:}
openai.model=${OPENAI_MODEL:gpt-5.1-chat-latest}

# CORS
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:5176,http://localhost:5177,http://localhost:3000,https://calm-demo.loca.lt}

# Jackson Configuration
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.default-property-inclusion=non_null

# JWT Configuration
jwt.secret=${JWT_SECRET:calm-admin-dev-secret-key-change-in-production-must-be-at-least-256-bits}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Admin User
admin.username=${ADMIN_USERNAME:admin}
admin.password=${ADMIN_PASSWORD:admin123}

# Rate Limiting
rate.limit.login.attempts=${RATE_LIMIT_LOGIN:5}
rate.limit.login.duration=${RATE_LIMIT_DURATION:60}
